<Page x:Class="WPF_Market.View.product"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:WPF_Market.Model"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      MinWidth="800"
      MinHeight="450"
      Title="product"
      ShowsNavigationUI="False">
    <Page.DataContext>
        <local:ProductModel></local:ProductModel>
    </Page.DataContext>
    
    <Border CornerRadius="10"
            BorderThickness="3"
            Background="White">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="1">
                <WrapPanel x:Name="Container"
                    Grid.Row="1">
                    <WrapPanel.Resources>
                        <ControlTemplate x:Key="ProductTemplate">
                            <Border BorderBrush="#37B89C"
                                    CornerRadius="10"
                                    BorderThickness="2"
                                    Margin="5,5,5,5"
                                    >
                                
                          
                            <Button BorderThickness="0"
                                    BorderBrush="Transparent"
                                Background="Transparent"
                                Width="160"
                                Height="260"
                                Margin="0"
                                    Command="{Binding SeeDetailCommand}"
                                    CommandParameter="{Binding}"
                                    >
                                <StackPanel>
                                    <Grid>
                                        <Image x:Name="Illustration"
                                              Source="{Binding Image_link}"
                                               MaxHeight="155"
                                               MaxWidth="160"
                                            Stretch="Fill"/>
                                    </Grid>
                                    <TextBlock Text="{Binding Txt_NameProduct}"
                                            TextTrimming="WordEllipsis"
                                            FontWeight="SemiBold"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            FontSize="15"></TextBlock>
                                    <TextBlock 
                                            Text="{Binding Txt_Price}"
                                            TextTrimming="WordEllipsis"
                                            FontWeight="Medium"
                                            FontSize="18"
                                            Foreground="#EE4D2D"
                                            HorizontalAlignment="Center"></TextBlock>
                                    <materialDesign:RatingBar               
                                           HorizontalAlignment="Center"  
                                           materialDesign:ColorZoneAssist.Foreground="Yellow"
                                           Foreground="LightYellow"                                  
                                           x:Name="mtr_ratingbar"
                                           Max="5"
                                           Min="1"
                                           IsReadOnly="True"
                                           Value="{Binding Rating}"  
                                     />
                                    <TextBlock x:Name="txt_soluongdaban"
                                            Text="{Binding Txt_Sold}"
                                            TextTrimming="WordEllipsis"
                                            Margin="0,0,0,0"
                                            HorizontalAlignment="Center"></TextBlock>
                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Center">
                                        <materialDesign:PackIcon Kind="MapMarker" 
                                                                 Foreground="Green"/>
                                        <TextBlock Text="{Binding Txt_DiaDiem}"
                                                TextTrimming="WordEllipsis"
                                                HorizontalAlignment="Center"
                                                Foreground="#696969"></TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Button>
                            </Border>
                        </ControlTemplate>
                        <Style TargetType="Button"></Style>
                    </WrapPanel.Resources>

                </WrapPanel>


            </ScrollViewer>
        </Grid>
    </Border>
</Page>
